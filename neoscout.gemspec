require 'lib/version'

Gem::Specification.new do |s|
  s.name     = 'neoscout'
  s.version  = NeoScout::VERSION
  s.summary  = 'Helper gem for extraction and validation of graph database schemas'
  s.author   = 'stefanp@moviepilot.com'
  s.files    = 'lib/**/*'
  # s.rubyforge_project = 'neoscout'
end